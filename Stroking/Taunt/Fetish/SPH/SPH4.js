{
    if (tryRunTauntFetchId()) {
        sendMessage(random('I don\'t know if this ', 'Maybe this ') + 'is ' + random('difficult ', 'hard ') + 'for you to accept, %PetName%');
        sendMessage('But ' + random('that ', 'your ') + random('penis ', 'cock ', 'dick ') + 'if ' + random('you can call it that ', 'it deserves to be called that ') + 'is ' + random('so tiny', 'pathetic', 'so small', 'so small and pathetic'));

        if(!isInChastity()) {
            //Save state before
            let wasStroking = pauseStroking();

            smallCBTPunishment(false, true);

            resumeStroking(wasStroking);
        }

        sendMessage(random('Denial and humiliation ', 'Abuse and derision ') + 'that\'s all it\'s good for');
        sendMessage('Not ' + random('sex ', 'fucking ') + 'not ' + random('blowjobs ', 'getting sucked ') + 'or any of the things big cocks get');
        sendMessage('Your ' + random('ridiculous ', 'pathetic ', 'tiny ') + 'little ' + random('dicklet ', 'cock ', 'penis ') + 'just gets ' + random('to feel your hand ', 'your hands wrapped around ') + 'instead of lips');

        if(SISSY_LIMIT.isAllowed()) {
            sendMessage('It\'s not like you need it as a sissy anyway %Lol%');
            sendMessage('You are just fine with your small limp useless clit %Wicked%');
        }
    }
}