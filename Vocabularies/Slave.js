function slaveVocabulary() {
    const answers = ["slave", "slave", "slave", "worthless slave", "cum craving slave", "cock craving slave", "owned slave", "slave boy", "servant", "chained slave"];
    return answers[randomInteger(0, answers.length - 1)];
}