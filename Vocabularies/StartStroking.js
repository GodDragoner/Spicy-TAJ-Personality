function startStrokingVocabulary() {
    if(!isInChastity()) {
       return '%StartStrokingCock%';
    } else {
        return random('Put the vibrator on that %ChastityCage%', 'Start vibrating that %ChastityCage%', 'You need to start vibrating that %ChastityCage%') + random('', ' %SlaveName%');
    }
}