{
    sendMessage("Go ahead %SlaveName% and start stroking that %Cock%");
    startStroking(60);

    const randomModule = randomInteger(0, 100);
    switch(randomModule) {
        case 0:
            break;

    }
}