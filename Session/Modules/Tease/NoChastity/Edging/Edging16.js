{
    startEdging();
    sendMessage('%LetEdgeFade%');

    startEdging(getEdgeHoldSeconds());
    sendMessage('Relax and breathe, %SlaveName%');
    sendMessage('There are a few more edges coming your way %Grin%');
    startEdging(getEdgeHoldSeconds());
    sendMessage('I\'m going to make you hold each and every one of these edges');

    if (sendYesOrNoQuestion('You do enjoy doing that, right?')) {
        sendMessage(' Then let\'s get to it! %EmoteHappy%');
    } else {
        sendMessage('Oh? Well, I\'m sure you\'re not going to disappoint me %Smile%');
    }

    startEdging(getEdgeHoldSeconds());
    sendMessage('I\'ll show you some "cool down" pictures in between %Lol%', 0);
    showTeaseImage(3);
    showTeaseImage(3);
    startEdging(getEdgeHoldSeconds());
    showTeaseImage(3);
    showTeaseImage(3);
    showTeaseImage(3);
    startEdging(getEdgeHoldSeconds());
    showTeaseImage(3);
    showTeaseImage(3);
    startEdging(getEdgeHoldSeconds());
    showTeaseImage(3);
    showTeaseImage(3);
    showTeaseImage(3);
    showTeaseImage(3);
    startEdging(getEdgeHoldSeconds());
    showTeaseImage(3);
    startEdging(getEdgeHoldSeconds());
    showTeaseImage(3);
    showTeaseImage(3);
    showTeaseImage(3);
    startEdging(getEdgeHoldSeconds());
    showTeaseImage(3);
    startEdging(getEdgeHoldSeconds());
    showTeaseImage(3);
    showTeaseImage(3);
    startEdging(getEdgeHoldSeconds());
    showTeaseImage(3);
    showTeaseImage(3);
    showTeaseImage(3);
    startEdging(getEdgeHoldSeconds());
    sendMessage('%LetEdgeFade%');

}