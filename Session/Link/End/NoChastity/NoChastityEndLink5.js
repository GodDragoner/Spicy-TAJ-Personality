{
    if (tryRunEndLinkFetchId()) {
        sendMessage('Almost at the end once again');
        sendMessage('But we can squeeze in a few more edges I think %Grin%');

        startMultipleEdges(3);
        sendMessage('%LetEdgeFade%');
    }
}
