{
    if(tryRunOrgasmFetchId()) {
        startMultipleEdges(5);

        startEdging(getEdgeHoldSeconds(EDGE_HOLD_MEDIUM), true, EDGE_END_ORGASM);
        waitForCumAnswer();

        if (shouldCEI()) {
            sendEatInstructions();
        }

        sendMessage(random('It\'s a shame of all that built up frustration', 'Too bad that throbbing ache is gone now', 'And gone is all that aching'));
    }
}